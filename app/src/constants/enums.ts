export enum Roles {
  ADMIN = 'ADMIN',
  USER = 'USER'
}

export enum SubscriptionType {
  NEWS = 'NEWS',
  CODE = 'CODE',
  GENERAL = 'GENERAL'
}

export enum NavRoutes {
  HOME = 'home',
  AUTH = 'auth',
  WORK = 'work',
  PROFILE = 'profile',
  EMAILS = 'emails'
}

export enum NavRouteNames {
  HOME = 'Home',
  AUTH = 'Login',
  WORK = 'Work',
  PROFILE = 'Profile',
  EMAILS = 'Emails'
}
