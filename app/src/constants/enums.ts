export enum Roles {
  ADMIN = 1,
  USER = 2
}

export enum SubscriptionType {
  NEWS = 'news',
  CODE = 'code',
  GENERAL = 'general'
}

export enum NavRoutes {
  HOME = 'home',
  AUTH = 'auth',
  WORK = 'work',
  PROFILE = 'profile',
  EMAILS = 'emails'
}

export enum NavRouteNames {
  HOME = 'Home',
  AUTH = 'Login',
  WORK = 'Work',
  PROFILE = 'Profile',
  EMAILS = 'Emails'
}
